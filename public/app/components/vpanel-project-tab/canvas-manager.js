'use strict';

export default class CanvasManager {

  canvasMeasureChanged(dim) {

  }

  componentMeasureChanged(uiComponent, component, dim) {

  }

  addBindingChangedListener(handler) {
    // return unsubscribe;
    return () => {};
  }

  bindingPath(binding) {

  }
};